import { Observable } from 'tns-core-modules/data/observable';
import { AutoCompleteTextField } from 'nativescript-auto-complete-text-field';

export class HelloWorldModel extends Observable {

    constructor() {
        super();
    }
}
